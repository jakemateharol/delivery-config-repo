server:
  port: 8081

spring:
  application:
    name: orders-service
  datasource:
    url: jdbc:mysql://mysql-db:3306/orders_db_dev
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://registry-server:8761/eureka/
