server:
  port: 8086

spring:
  application:
    name: tracking-service

  datasource:
    # 🔧 CORREGIDO: de localhost:3304 a mysql-db:3306
    url: jdbc:mysql://mysql-db:3306/tracking_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      # 🔧 CORREGIDO: de localhost a registry-server
      defaultZone: http://registry-server:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: ${spring.application.name}
